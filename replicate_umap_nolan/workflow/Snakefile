rule all:
    input:
        "workflow/scripts/run.py"
    output:
        "results/output.pdf"
    threads: 32
    resources:
        runtime=3
    shell:
        "workflow/envs/venv/bin/python {input}"
